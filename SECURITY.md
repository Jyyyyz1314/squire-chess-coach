# 安全策略

## 报告漏洞

公开仓库启用 GitHub Private Vulnerability Reporting 后，请通过仓库的 **Security → Report a vulnerability** 私下报告。启用前，请联系项目维护者并避免在公开 Issue 中披露漏洞细节。

报告应包含影响、复现步骤、受影响版本和建议修复方式。请勿附带真实 API 密钥、完整用户棋谱或个人信息。

## 密钥与数据

- AI 模型采用用户自带密钥（BYOK）：Key 默认只在当前浏览器会话保存；用户选择“在这台设备上记住”后才写入该浏览器的 localStorage。共享设备不应启用持久保存。
- Key 随主动讲解请求经过同源代理转发给用户指定的模型供应商，只在服务端请求内存中短暂存在；应用不得记录请求体、持久化 Key 或缓存跨用户回答。
- `LICHESS_SESSION_SECRET` 必须是至少 32 字符的独立随机值；不得复用模型密钥或提交到 Git。
- `.env.local`、导出的用户棋谱和运行时缓存不应提交到 Git。
- 泄露密钥后应立即在模型供应商处撤销并轮换；仅从 Git 历史删除并不够。
- AI 老师请求会把当前 FEN、候选变化、开局名和用户问题发送给所配置的模型供应商；普通棋盘分析由浏览器内的 Stockfish 完成。
- 自定义模型地址仅接受 HTTPS 公网端点，并拒绝 localhost、私网与链路本地地址；正式高风险部署仍建议使用供应商白名单和出口网络策略防御 DNS 重绑定。
- Lichess access token 只保存在加密的 HttpOnly Cookie 中。用户断开连接时会尝试在 Lichess 撤销令牌，并始终删除本地会话 Cookie。

## 支持范围

安全更新当前只维护主分支最新版本。正式发布后改为按版本列出支持周期。
